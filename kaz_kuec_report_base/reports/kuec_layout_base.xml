<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <template id="external_layout_standard">

        <div t-attf-class="header o_company_#{company.id}_layout">
            <div class="d-flex justify-content-between align-items-center mb-1">
                <img t-if="company.logo" class="o_company_logo_small" t-att-src="image_data_uri(company.logo)"
                     alt="Logo"/>
<!--                <div t-if="company.report_header" t-field="company.report_header"-->
<!--                     class="o_company_tagline mw-50 fw-bold">Company tagline-->
<!--                </div>-->
            </div>
        </div>
        <t t-set="layout_background_url"
           t-value="'data:image/png;base64,%s' % company.layout_background_image.decode('utf-8') if company.layout_background_image and company.layout_background == 'Custom' else
                '/base/static/img/demo_logo_report.png' if company.layout_background == 'Demo logo' else ''"/>
        <div t-attf-class="article o_report_layout_standard o_table_standard o_company_#{company.id}_layout o_snail_mail {{'o_report_layout_background' if company.layout_background != 'Blank' else ''}}"
             t-attf-style="{{ 'background-image: url(%s);' % layout_background_url if layout_background_url else '' }}"
             t-att-data-oe-model="o and o._name"
             t-att-data-oe-id="o and o.id"
             t-att-data-oe-lang="o and o.env.context.get('lang')">
            <link rel="stylesheet" href="/kaz_kuec_report_base/static/src/css/font.css"/>
            <t t-out="0"/>
        </div>
        <div t-attf-class="footer o_company_#{company.id}_layout {{report_type != 'pdf' and 'mt-auto'}}">
            <link rel="stylesheet" href="/kaz_kuec_report_base/static/src/css/font.css"/>
            <div class="o_footer_content border-top pt-2" style="font-size: 12px;">
                <div class="d-flex justify-content-between">
                    <div t-if="report_type == 'pdf'">
                        Document No:
                        <t t-set="sequence_field" t-value="sequence_field or 'sequence'"/>
                        <span class="ms-1" t-out="o[sequence_field]"/>
                    </div>

                    <div t-if="report_type == 'pdf'" style="white-space: nowrap;">
                        Print Date:
                        <span class="ms-1" t-esc="datetime.datetime.now().strftime('%d-%m-%Y')"/>
                    </div>
                </div>

                <div class="flex-grow-1 text-center me-2">
                    <span t-esc="company.partner_id.contact_address"/>
                    <br/>
                    <span class="ms-1 blue-highlight-color" t-field="company.hr_email_address"/>
                    <span class="ms-1 fw-bold">|</span>
                    <span class="ms-1 blue-highlight-color" t-field="company.website"/>
                </div>

                <div class="text-center text-muted">
                    <div t-if="report_type == 'pdf' and show_page" class="text-nowrap">Page
                        <span class="page"/>
                        /
                        <span class="topage"/>
                    </div>
                </div>

            </div>
        </div>
    </template>

    <template id="external_layout">
        <t t-if="not o" t-set="o" t-value="doc"/>

        <t t-if="not company">
            <!-- Multicompany -->
            <t t-if="company_id">
                <t t-set="company" t-value="company_id"/>
            </t>
            <t t-elif="o and 'company_id' in o and o.company_id.sudo()">
                <t t-set="company" t-value="o.company_id.sudo()"/>
            </t>
            <t t-else="else">
                <t t-set="company" t-value="res_company"/>
            </t>
        </t>

        <t t-call="kaz_kuec_report_base.external_layout_standard">
            <t t-out="0"/>
        </t>
    </template>
</odoo>