<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <record id="view_petty_confirm_settlement_form" model="ir.ui.view">
        <field name="name">petty.cash.request Confirm Settlement Form</field>
        <field name="model">petty.cash.request</field>
        <field name="priority">100000000</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <div>
                        <p>
                            Press <b>CONFIRM</b> to reconcile the petty cash.
                            <br/>
                        </p>
                    </div>
                </sheet>
                <footer>
                    <button name="confirm_settlement" type="object" string="Confirm" class="oe_highlight"/>
                    <button class="btn btn-secondary oe_button oe_form_button" special="cancel" string="Cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="view_petty_request_form" model="ir.ui.view">
        <field name="name">petty.cash.request Form</field>
        <field name="model">petty.cash.request</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="action_submit_request" type="object" invisible="state!='new'" class="oe_highlight"
                            string="Submit"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,approval,complete"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="name" no_label="1" readonly="1" force_save="1"/>
                        </h1>
                    </div>
                    <group string="Request Info">
                        <group>
                            <field name="by_employee" readonly="state != 'new'"/>
                            <field name="requester_emp_id" invisible="not by_employee"
                                   readonly="state != 'new'"/>
                            <field name="requester_partner_id" invisible="by_employee"
                                   readonly="state != 'new'"/>
                            <field name="narration" placeholder="Petty cash expenses of ..."
                                   readonly="state != 'new'"/>
                        </group>
                        <group>
                            <field name="order_date" readonly="state != 'new'"/>
                            <field name="approve_date" readonly="1"/>
                        </group>
                        <group>
                            <field name="amount" readonly="state != 'new'"/>
                        </group>
                        <group>
                            <field name="settlement_amount"/>
                            <field name="different_amount"/>
                        </group>
                    </group>
                    <notebook>
                        <page name="settlement" string="Settlement" groups="account.group_account_user"
                              invisible="state!='complete'">

                            <field name="settlement_ids" widget="one2many_list" nolabel="1" colspan="4"
                                   readonly="state!='complete'">
                                <list string="Settlement" editable="bottom">
                                    <field name="petty_request_id" column_invisible="1"/>
                                    <field name="bill_id"/>
                                    <field name="expense_id"/>
                                    <field name="product_id"
                                           required="bill_id and expense_id"
                                           context="{'default_can_be_expensed': 1}"/>
                                    <field name="name"/>
                                    <field name="expense_account_id"
                                           readonly="bill_id or expense_id"/>
                                    <field name="amount"/>
                                </list>
                            </field>

                            <field name="attachment_ids" widget="many2many_binary" string="Attach a file"
                                   class="oe_inline"/>

                        </page>
                    </notebook>
                    <separator string="Notes" colspan="4"/>
                    <field name="note" placeholder="Notes..."/>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="view_petty_approval_form" model="ir.ui.view">
        <field name="name">petty.cash.approval Form</field>
        <field name="model">petty.cash.request</field>
        <field name="priority">1000000</field>
        <field name="arch" type="xml">
            <form>
                <field name="in_settlement" invisible="1"/>
                <field name="settlement_state" invisible="1"/>
                <field name="has_request_not_reconciled" invisible="1"/>
                <header>
                    <button name="action_approve" type="object" string="Approve" class="btn-primary"
                            invisible="state!='draft'" groups="account.group_account_user"/>
                    <button name="action_reject" type="object" string="Reject" class="btn-default"
                            invisible="state!='draft'" groups="account.group_account_user"/>
                    <button name="action_draft" type="object" string="Reset to Draft" class="btn-default"
                            invisible="state!='reject'" groups="account.group_account_user"/>
                    <button name="open_wizard_operation" type="object" string="Register Petty Cash" class="btn-primary"
                            invisible="state!='approval'" groups="account.group_account_user"/>
                    <button name="petty_cash_settlement"
                            type="object"
                            string="Reconcile" class="btn-primary"
                            invisible="state!='complete' or in_settlement == False"
                            groups="account.group_account_user"/>

                    <button name="open_wizard_operation" type="object"
                            string="Return Petty Cash Remaining" class="btn-default"
                            invisible="state != 'complete' or settlement_state != 'partial'"
                            groups="account.group_account_user"/>
                    <button name="open_wizard_operation" type="object" invisible="1"
                            string="Register Petty Cash Intemperance" class="btn-default"
                            groups="account.group_account_user"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,approval,complete"/>
                </header>

                <div class="alert alert-warning mb-0" role="alert" invisible="not has_request_not_reconciled">
                    <strong>Warning:</strong>
                    This request cannot be approved because this partner has already petty cash isn't full reconciled
                </div>

                <sheet>

                    <div name="button_box" class="oe_button_box">
                        <button name="open_reconcile_view" class="oe_stat_button" icon="fa-bars" type="object"
                                invisible="state!='complete'" string="Reconciled Entries">
                        </button>
                    </div>

                    <widget name="web_ribbon" title="Partial Settlement" invisible="settlement_state != 'partial'"/>
                    <widget name="web_ribbon" title="Full Settlement" invisible="settlement_state != 'full'"/>

                    <div class="col-12 oe_centered text-center">
                        <h2>
                            <field name="different_amount" readonly="1"/>
                            -
                            <field name="narration" readonly="1"/>
                            (
                            <field name="name" no_label="1" readonly="1" force_save="1"/>
                            )
                        </h2>
                    </div>

                    <group string="Request Information">
                        <group>
                            <field name="by_employee" invisible="1"/>
                            <field name="requester_emp_id" readonly="1" invisible="not by_employee"/>
                            <field name="requester_partner_id" readonly="1" invisible="not by_employee"/>
                        </group>
                        <group readonly="1">
                            <field name="order_date" readonly="1"/>
                            <field name="approve_date" readonly="1"/>
                        </group>
                        <group>
                            <field name="amount" readonly="1"/>
                        </group>
                        <group>
                            <field name="settlement_amount" readonly="1"/>
                        </group>
                    </group>
                    <group>
                        <group string="Accounting Information" groups="account.group_account_user">
                            <field name="petty_cash_journal_id"
                                   options="{'no_open': True, 'no_create': True}"
                                   groups="account.group_account_user"/>
                            <field name="operation_journal_id" widget="selection" groups="account.group_account_user" readonly="1"/>
                            <field name="operation_account_id"
                                   groups="account.group_account_user"/>
                            <field name="move_id" readonly="1" groups="account.group_account_user"/>
                        </group>
                        <group string="Analytic Information" groups="account.group_account_user">
                            <field name="use_analytic" groups="account.group_account_user"/>
                            <field name="analytic_distribution" widget="analytic_distribution"
                                   invisible="not use_analytic"
                                   groups="analytic.group_analytic_accounting"/>
                        </group>
                    </group>
                    <notebook>
                        <page name="settlement" string="Settlement" groups="account.group_account_user"
                              invisible="state!='complete'">
                            <field name="settlement_ids" widget="one2many_list" nolabel="1" colspan="4"
                                   readonly="state!='complete'">
                                <list string="Settlement" editable="bottom">
                                    <field name="petty_request_id" column_invisible="1"/>
                                    <field name="bill_id"/>
                                    <field name="expense_id"/>
                                    <field name="product_id"
                                           required="not bill_id and not expense_id"
                                           context="{'default_can_be_expensed': 1}"/>
                                    <field name="name"/>
                                    <field name="expense_account_id" force_save="1"
                                           readonly="bill_id or expense_id"/>
                                    <field name="amount"/>
                                </list>
                            </field>

                            <field name="attachment_ids" widget="many2many_binary" string="Attach a file"
                                   class="oe_inline"/>
                        </page>
                    </notebook>
                    <separator string="Notes" colspan="4"/>
                    <field name="note" placeholder="Notes..."/>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="view_petty_request_list" model="ir.ui.view">
        <field name="name">petty.cash.request list</field>
        <field name="model">petty.cash.request</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="narration"/>
                <field name="amount"/>
                <field name="settlement_state" widget="badge" decoration-info="settlement_state == 'no'"
                       decoration-warning="settlement_state == 'partial'"
                       decoration-success="settlement_state == 'full'"/>
                <field name="state"/>
            </list>
        </field>
    </record>

    <record id="view_petty_approval_list" model="ir.ui.view">
        <field name="name">petty.cash.approval list</field>
        <field name="model">petty.cash.request</field>
        <field name="priority">1000000</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="narration"/>
                <field name="amount"/>
                <field name="settlement_amount"/>
                <field name="different_amount"/>
                <field name="settlement_state" widget="badge" decoration-info="settlement_state == 'no'"
                       decoration-warning="settlement_state == 'partial'"
                       decoration-success="settlement_state == 'full'"/>
                <field name="state"/>
            </list>
        </field>
    </record>

    <record id="action_petty_request_open_my" model="ir.actions.act_window">
        <field name="name">My Petty Cash Requests</field>
        <field name="res_model">petty.cash.request</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[('user_id', '=', uid)]</field>
    </record>

    <record id="action_petty_open_approval" model="ir.actions.act_window">
        <field name="name">Approval</field>
        <field name="res_model">petty.cash.request</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[('state', '!=', 'new')]</field>
        <field name="context">{'group_by':['requester_partner_id']}</field>
    </record>

    <record id="action_petty_open_approval_view_list" model="ir.actions.act_window.view">
        <field name="sequence" eval="1"/>
        <field name="view_mode">list</field>
        <field name="view_id" ref="view_petty_approval_list"/>
        <field name="act_window_id" ref="action_petty_open_approval"/>
    </record>

    <record id="action_petty_open_approval_view_form" model="ir.actions.act_window.view">
        <field name="sequence" eval="2"/>
        <field name="view_mode">form</field>
        <field name="view_id" ref="view_petty_approval_form"/>
        <field name="act_window_id" ref="action_petty_open_approval"/>
    </record>

    <menuitem id="menu_open_petty_request_my"
              parent="expense_funding.menu_petty_orders"
              action="action_petty_request_open_my"
              sequence="1"/>

    <menuitem id="menu_open_petty_approval"
              parent="expense_funding.menu_petty_orders"
              action="action_petty_open_approval"
              groups="account.group_account_user"
              sequence="2"/>

</odoo>
