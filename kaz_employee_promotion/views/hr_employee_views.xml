<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!--The view inherits from the existing "hr.view_employee_form" view
       and adds a new page called "Promotions" with a field named "promotion_ids". -->
    <record id="view_employee_form" model="ir.ui.view">
        <field name="name">hr.employee.view.form.inherit.employee.promotion</field>
        <field name="model">hr.employee</field>
        <field name="inherit_id" ref="hr.view_employee_form"/>
        <field name="arch" type="xml">

            <div name="button_box" position="inside">
                <button name="action_open_employee_promotions" class="oe_stat_button" icon="fa-level-up" type="object"
                        invisible="not promotion_ids">
                    <field name="promotion_ids" invisible="1"/>
                    <div class="o_stat_info">
                        <span class="o_stat_text">Employee Promotions</span>
                    </div>
                </button>
            </div>

        </field>
    </record>
</odoo>
