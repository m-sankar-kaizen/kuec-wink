<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <record id="view_budget_post_form" model="ir.ui.view">
        <field name="name">account.budget.post.form</field>
        <field name="model">account.budget.post</field>
        <field name="arch" type="xml">
            <form string="Budgetary Position">
                <sheet>
                    <group col="4">
                        <field name="name"/>
                        <field name="company_id"
                               groups="base.group_multi_company"
                               options="{'no_create': True}"/>
                    </group>
                    <notebook>
                        <page string="Accounts">
                            <field name="account_ids">
                                <list>
                                    <field name="code"/>
                                    <field name="name"/>
                                </list>
                            </field>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_budget_post" model="ir.actions.act_window">
        <field name="name">Budgetary Position</field>
        <field name="res_model">account.budget.post</field>
        <field name="view_mode">list,form</field>
    </record>

    <menuitem
            id="menu_budget_post"
            name="Budgetary Position"
            parent="account.menu_analytic_accounting"
            action="action_budget_post"
            sequence="185"/>

</odoo>