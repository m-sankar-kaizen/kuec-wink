<?xml version="1.0" encoding="utf-8" ?>
<odoo noupdate="1">

    <!-- ================================
         Scheduled Action: Auto DB Backup
         ================================
         This cron job calls the `_schedule_auto_backup` method
         in the `db.backup.configure` model every 1 day.
         It is meant to automate the process of backing up the
         Odoo database on a daily basis.
    -->

    <record id="ir_cron_auto_db_backup" model="ir.cron">
        <field name="name">Backup : Automatic Database Backup</field>
        <field name="model_id" ref="model_db_backup_configure"/>
        <field name="state">code</field>
        <field name="code">model._schedule_auto_backup()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="active">True</field>
    </record>
</odoo>
