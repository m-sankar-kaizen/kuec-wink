<?xml version="1.0" encoding="UTF-8" ?>
<odoo noupdate="1">

    <record id="ir_cron_contact_attachment_expiry_reminder" model="ir.cron">
        <field name="name">Contact Attachment Expiry Reminder</field>
        <field name="model_id" ref="kaz_contact_approval.model_partner_attachment_line"/>
        <field name="state">code</field>
        <field name="code">model.action_attachment_expiry_reminder()</field>
        <field name="active" eval="True"/>
        <field name="interval_type">days</field>
        <field name="interval_number">1</field>
    </record>

</odoo>