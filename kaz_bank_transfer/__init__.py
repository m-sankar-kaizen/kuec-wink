# -*- coding: utf-8 -*-
"""
Bank Transfer Module Initialization

This file is responsible for initializing the `kaz_bank_transfer` module.
It imports the models subpackage which includes all the model definitions
such as `bank.transfer`, `bank.support.documents`, etc.

Make sure that all models are defined inside the `models/` directory and
are properly imported inside `models/__init__.py`.
"""

from . import models
